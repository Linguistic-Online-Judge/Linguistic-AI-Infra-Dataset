Reproduce the token-level MISC.Translit annotation style used by UD Chinese-GSDSimp. This is corpus annotation matching, not a request to silently correct or normalize source annotations. Let N be the number of entries in input.tokens. Return exactly one string for each token in the same order, so the transliterations array has exactly N items. Never split, merge, omit, add, or reorder token positions.

For Chinese, use lowercase Hanyu Pinyin with tone marks and no spaces inside a token; use ASCII apostrophes for syllable boundaries before zero-initial syllables. Preserve numbers and Latin-script text, including case. Follow the treebank's punctuation transliteration conventions: （ becomes (, ） becomes ), ， and 、 become ,, 。 becomes ., ： becomes :, ； becomes ;, 《 becomes «, 》 becomes », 〈 becomes <, and 〉 becomes >. Other punctuation must remain token-aligned. Rare characters may remain untransliterated when that is how the source annotation represents them.

Return exactly one JSON object in this shape: {"transliterations":["VALUE_1","VALUE_2"]}. The transliterations array must contain exactly N strings in input order. Do not output token text, indices, explanations, markdown, reasoning, confidence values, or extra keys. Before answering, silently verify that the JSON is valid and the array length equals N.
