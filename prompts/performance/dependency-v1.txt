Analyze the syntactic dependencies of the input tokens. Preserve every token_id. Return only a JSON object with an arcs array. Each arc must contain token_id, head_id, and deprel. Use head_id 0 and lowercase root for the sentence root; otherwise use an existing token_id as the head_id. Return exactly one arc per input token, with no explanations, code fences, or additional fields.
